database:
  image: postgres
redis:
  image: redis
uwsgi:
  build: .
  volumes:
    - .:/code
  links:
   - database
   - redis
  ports:
   - "80:8000"
   - "8000:8000"
  environment:
   - DJANGO_SETTINGS_MODULE=cachelayer.settings.docker
  command: uwsgi --ini uwsgi.ini